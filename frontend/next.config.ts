import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hide the Next.js dev-tools floating indicator (the "N" button) in dev mode.
  devIndicators: false,
};

export default nextConfig;
